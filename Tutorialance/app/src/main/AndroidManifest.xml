<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="tutorialance.widevision.com.tutorialance">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="ANDROID.PERMISSION.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".activity.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/app_icon"
        android:installLocation="preferExternal"
        android:label="@string/app_name"
        android:theme="@style/Theme.DesignDemo"
        tools:replace="android:icon">
        <meta-data
            android:name="AA_DB_NAME"
            android:value="tutorialance.db" />
        <meta-data
            android:name="AA_DB_VERSION"
            android:value="1" />

        <activity
            android:name=".activity.SplashActivity"
            android:configChanges="orientation"
            android:label="@string/app_name"
            android:screenOrientation="sensorPortrait"
            android:theme="@android:style/Theme.Translucent">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.MainActivity"
            android:configChanges="orientation"
            android:screenOrientation="sensorPortrait" />
        <activity
            android:name=".activity.QuizFragment"
            android:configChanges="orientation"
            android:screenOrientation="sensorPortrait"></activity>
        <activity
            android:name=".activity.ResultFragment"
            android:configChanges="orientation"
            android:screenOrientation="sensorPortrait"></activity>
        <activity
            android:name=".activity.QuizLayoutActivity"
            android:configChanges="orientation"
            android:screenOrientation="sensorPortrait"></activity>
        <activity
            android:name=".activity.FeedbackActivity"
            android:configChanges="orientation"
            android:screenOrientation="sensorPortrait"></activity>
        <activity
            android:name=".activity.LessonActivity"
            android:configChanges="orientation"
            android:screenOrientation="sensorPortrait"></activity>
    </application>

</manifest>
